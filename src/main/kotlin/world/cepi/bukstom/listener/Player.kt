package world.cepi.bukstom.listener

/*
fun onPlayerJoin(ev: PlayerLoginEvent) {
	Bukkit.getPluginManager().callEvent(PlayerJoinEvent(MinestomPlayer(ev.player, Bukkit.getServer() as MinestomServer), Component.empty()))
}
*/

/*
fun onPlayerChat(ev: PlayerChatEvent) {
	ev.isCancelled = true

	Bukkit.getPluginManager().callEvent(
		AsyncChatEvent(
			true,
			MinestomPlayer(ev.player, Bukkit.getServer() as MinestomServer),
			setOf(
				Audiences.all()
			),
			ChatRenderer.defaultRenderer(),
			Component.text(ev.message),
			Component.text(ev.message)
		)
	)
}*/
